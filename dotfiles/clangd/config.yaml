 If:
   PathMatch: .*\.cpp

CompileFlags:
  Add: [-std=c++23, -Wall, -Wextra, -DUwU]

---

If:
  PathMatch: .*\.c

CompileFlags:
  Add: [
    -xc,
    -std=c17,
    -Werror=missing-braces,
    -DMYLOCAL
  ]
